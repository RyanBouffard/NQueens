running instructions:
	import into java IDE
	for backtracking:
		run from NQueenProblemBackTracking.java and follow the prompt.
	for forward checking:
		run from NQueenProblemForwardChecking.java and follow the prompt.